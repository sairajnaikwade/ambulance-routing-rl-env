[project]
name = "ambulance-env"
version = "0.1.0"
description = "Ambulance Routing RL Environment using OpenEnv"
authors = [
  { name = "Bhushan Korde" }
]
dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic",
  "openai",
  "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"

[tool.openenv]
env = "server.env:AmbulanceEnv"
